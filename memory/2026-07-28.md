# 2026-07-28 工作记忆

## 用户管理模块（新增）
- **后端**：新增 `routers/users.py`，包含用户 CRUD（GET/POST/PUT/DELETE /api/v1/users）+ 权限分配（PUT /api/v1/users/{id}/permissions），路由已注册到 `main.py`
- **模型**：`models.py` 新增 `UserPermission` 表（user_id, module_key, can_read, can_write）
- **Schema**：`schemas.py` 新增 UserCreate/UserUpdate/UserOut/UserWithPermissions/UserPermissionOut/UserPermissionUpdate
- **前端**：`index.html` 侧边栏新增「用户管理」入口（仅 admin 可见），完整页面含表格+新增/编辑/权限分配弹窗，`checkModulePermission()` 函数前端控制按钮显隐
- **Auth**：`auth.py` 新增 `check_module_access(module_key, write=False)` 依赖工厂，admin 免检，其他角色查 user_permissions 表；`login API` 返回新增 `permissions` 字段包含模块权限列表

## 模块权限挂载（进行中）
- 计划将 `check_module_access` 逐一挂载到各模块路由的读写端点
- 已确认 `devices.py` 已导入 `check_module_access`，其他路由文件尚未修改
- 所有路由文件列表：devices.py, production.py, servers.py, agingracks.py, wifi.py, mes.py, antivirus.py, network.py, dashboard.py, projects.py
- API 验证：新增用户、删除用户、权限分配接口已全部通过测试
# 2026-07-28 工作记忆（续）

## 模块权限挂载（卡片已完成，需要写代码但被限制）
**状态：已设计完方案，代码改动被会话 flush 限制阻断，需下个会话继续**

### 已完成部分
- `auth.py` 已添加 `check_module_access()` 依赖工厂函数
- `routers/users.py` 已创建（用户 CRUD + 权限管理 API）
- `main.py` 已注册 users 路由
- `index.html` 已添加用户管理页面、`checkModulePermission()` 前端函数
- `routers/devices.py` 已导入 `check_module_access`

### 待完成
需给每个模块路由文件中的读写端点添加 `dependencies=[Depends(check_module_access(module_key, write=True))]`

具体改动（每人路由文件）：
1. **devices.py** — 已导入 `check_module_access`，待给 GET/列表加读权限、POST/PUT/DELETE/import/export 加写权限
2. **production.py** — 导入 + 给 weekly/monthly 各端点加读/写权限
3. **servers.py** — 导入 + 加权限
4. **agingracks.py** — 导入 + 加权限
5. **wifi.py** — 导入 + 加权限
6. **mes.py** — 导入 + 给 orders/bugs/devreqs 各端点加权限（每个模块独立）
7. **antivirus.py** — 导入 + 加权限
8. **network.py** — ping 端点加读权限
9. **dashboard.py** — 看板接口加读权限
10. **projects.py** — 已有 require_role，加 check_module_access 替代或叠用

### 前端按钮显隐
`checkModulePermission(moduleKey, write)` 函数已在 `index.html` 中定义，调用各模块页面的工具栏按钮显隐逻辑已完成。

### 后续入口
下个会话直接继续挂载各路由文件的 `check_module_access` 依赖即可。也可考虑更简单的统一方案：在`main.py`的 APIRouter 级别用 middleware 统一判断路径前缀映射模块 key，但当前逐个路由加 dependencies 更稳妥、更显式。
